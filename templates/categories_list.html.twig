{% for cat in categories %}
    <li>
      <a href="{{ path('categorie_films', {
        'categorie': cat.name|lower|replace({' ': '-'}),
      }) }}">{{ cat.name|capitalize }}</a>
    </li>
{% endfor %}